# 树莓派信息面板 panel.py 设计笔记

> 这份笔记讲的是**为什么这么写**,而不是代码本身。
> 目标:以后想改面板、加功能时,知道**动哪里、为什么、会影响什么**,而不用从头读代码。
> 配套硬件:树莓派 5 + 2.0" ST7789V2 SPI 屏(320×240,32位 framebuffer)。

---

## 〇、整个面板的本质:一句话理解

**屏幕就是一块内存。你每隔一小段时间,画一整张图,把它转成屏幕认识的字节格式,写进 `/dev/fb0`,屏幕就显示出来。**

没有 Pygame、没有图形库后端、没有 GPU。就是"画图 → 写内存"的循环。理解这一点,整个程序就不神秘了。

```
循环:
  1. 采集数据(IP / CPU / 温度 / 内存)
  2. 用 Pillow 在一张 320×240 的图上画好所有东西
  3. 把图转成 framebuffer 的字节格式(BGRX)
  4. 写进 /dev/fb0
  5. 睡一小会儿,回到第 1 步
```

---

## 一、为什么用 Pillow 直接写 framebuffer,而不是 Pygame

### 当初的选择

Pygame 在**无桌面环境**下要用 SDL 的 framebuffer 后端(fbcon),在新系统(Ubuntu 24.04 + 内核 6.8)上配置麻烦、容易报错——和之前桌面模式那堆 GPU/SDL 报错是同一类坑。

而 Pillow 画图 → numpy 转字节 → 写 `/dev/fb0` 这条路:
- 不依赖任何图形后端,不需要 GPU
- 完全可控,出问题容易定位
- 更接近 framebuffer 的本质,学到的东西可迁移

### 改的时候要知道

如果以后换更强的屏/板子想要硬件加速,可以再考虑别的方案。但只要是"小屏 + 显示信息"这种需求,Pillow + framebuffer 永远够用且最省心。

---

## 二、framebuffer 字节格式:为什么要做 BGRX 转换

### 关键知识点

你的屏是 **32位 framebuffer**(`bits_per_pixel = 32`),每个像素 4 字节。但字节顺序不是直觉的 RGBA,而是 **BGRX**(蓝、绿、红、空)。

所以代码里这段是必须的:
```python
out[...,0]=arr[...,2]   # B <- R的位置其实是把R放到B通道
out[...,1]=arr[...,1]   # G
out[...,2]=arr[...,0]   # R
# out[...,3] 保持 0     # X(无用)
```
Pillow 画图用的是 RGB 顺序,framebuffer 要 BGR 顺序,所以**把 R 和 B 对调**。

### 为什么是 BGRX 而不是别的

这是当初阶段①验证出来的:第一次画一张深蓝底的图,如果显示正常(蓝是蓝)就说明 BGRX 对了;如果蓝变成红,就说明字节序要换。**你的屏验证下来是 BGRX。**

### 改的时候要知道

- 如果换了别的屏,颜色显示反了(红蓝互换),就是这里字节序的问题,把 `[...,0]` 和 `[...,2]` 的对应关系调一下。
- `bits_per_pixel` 不是 32 的屏(比如 16位 RGB565),这段转换要整个重写——16位是把 RGB 压进 2 字节,算法不同。先 `cat /sys/class/graphics/fb0/bits_per_pixel` 确认。

---

## 三、为什么要"采集慢、重绘快"

### 设计

- 数据每 **1 秒**采一次(`DATA_INTERVAL = 1.0`)
- 画面每秒重绘 **20 次**(`FPS = 20`)

### 为什么分开

如果数据和重绘都每秒一次,动画(进度条滑动、太阳旋转)就会一秒一跳,很卡。
分开后:数据慢慢更新,但画面高频重绘,**动画在两次数据之间平滑插值**,看起来就顺滑。

这是所有"实时仪表盘"的通用结构:**数据采集频率 ≠ 渲染帧率**,渲染要快才能动得顺。

### 改的时候要知道

- 觉得动画卡 → 提高 `FPS`(但太高费 CPU,20~30 够了)
- 觉得数据更新太频繁/太慢 → 调 `DATA_INTERVAL`
- 这两个值互相独立,改一个不影响另一个

---

## 四、平滑变化的核心:插值(lerp)

### 这是整个程序最重要的一个思想,用在两个地方

**1. 进度条平滑**
```python
disp[k] += (data[k]-disp[k])*SMOOTH
```
`disp` 是屏幕上**显示**的值,`data` 是**真实**的值。每帧让显示值朝真实值靠近一点(靠近的比例是 `SMOOTH`)。
- 真实值突然从 10% 跳到 80%,显示值不会瞬间跳,而是每帧 `+15%的差距`,几帧内平滑滑过去。
- `SMOOTH` 越小,滑得越慢越柔;越大越快越接近瞬变。

**2. 主题颜色过渡**
```python
theme_mix += (target-theme_mix)*THEME_SPEED
color = lerp(DAY_color, NIGHT_color, theme_mix)
```
`theme_mix` 是 0(纯日间)~1(纯夜间)。每帧朝目标爬一点。每个颜色 = 日间色和夜间色之间按 `theme_mix` 插值。
- 当 `theme_mix` 从 0 慢慢变到 1,**所有颜色一起平滑地从日间渐变到夜间**。

### lerp 函数本身
```python
def lerp(a,b,f): return tuple(int(a[i]+(b[i]-a[i])*f) for i in range(3))
```
意思:在 a 和 b 之间取一个点,f=0 时是 a,f=1 时是 b,f=0.5 时是正中间。
**这是计算机图形/动画里最基础的工具,记住它。**

### 改的时候要知道

- 进度条滑太快/太慢 → 调 `SMOOTH`
- 主题切换太快/太慢 → 调 `THEME_SPEED`
- 想给任何东西加"平滑变化",套这个公式:`显示值 += (目标值 - 显示值) * 速度`

---

## 五、动画的通用公式:时间 + sin/cos

### 所有"会动"的东西都靠这个

程序里有个一直累加的变量:
```python
anim += 1.0/FPS   # 每帧加一点,代表流逝的时间
```

然后用 `sin()` / `cos()` 把它变成**周期性的波动**:

- **太阳光芒旋转**:`角度 = anim * 0.6`,角度一直增大 = 一直转
- **太阳呼吸**:`半径 = 基础 * (1 + 0.12*sin(anim*2))`,sin 在 -1~1 之间振荡,所以半径周期性地涨缩
- **星星闪烁**:`亮度 = 0.5 + 0.5*sin(anim*3 + 相位)`,亮度周期性明暗
- **月亮浮动**:`y = cy + sin(anim*1.2)*1.5`,上下浮动

### 为什么用 sin

`sin` 输出一条平滑的波(-1 到 1 来回),用时间当输入,就得到**平滑的循环运动**。
- 乘以系数控制**幅度**(动多大):`sin(...)*1.5` 是上下浮动 1.5 像素
- 时间乘以系数控制**速度**(动多快):`sin(anim*3)` 比 `sin(anim*1)` 快 3 倍
- 加一个相位偏移让多个东西**错开**:三颗星星用 `+0, +1.5, +3` 的相位,就不会同时闪

### 改的时候要知道

**想加任何动效,就是这个套路:** 用 `anim` 当输入,`sin/cos` 当波形。
- 想要更快 → 加大时间系数
- 想要幅度更大 → 加大乘的系数
- 想要多个东西不同步 → 给每个加不同的相位偏移

---

## 六、按状态形变:太阳怎么"变身"月亮

### 原理

太阳和月亮画在**同一个位置**,用 `theme_mix` 控制各自的"强度":
```python
sun_a = 1 - mix    # 日间(mix=0)时太阳满强度
moon_a = mix       # 夜间(mix=1)时月亮满强度
```
- 太阳光芒的颜色乘以 `sun_a`:白天亮,过渡时渐淡,夜间消失
- 月亮的缺口、星星乘以 `moon_a`:白天没有,过渡时渐显,夜间完整

所以过渡时你看到的是:太阳光芒淡出 + 月亮缺口淡入,视觉上像"变身"。

### 月亮缺口怎么画的

月亮是"一个圆 + 一个偏移的背景色圆挖掉一块"=弯月。挖的那个圆用背景色,所以看起来像缺了一块。

### 改的时候要知道

- 想加别的"随状态变化"的元素,套这个思路:同一位置画两套,用状态值控制各自透明度/强度,此消彼长。

---

## 七、昼夜判断:为什么用本地小时

```python
is_day = 6 <= time.localtime().tm_hour < 18
```

### 为什么这么简单

按小时判断,不需要联网、不需要查日出日落,本地时间就够,可靠。6:00~18:00 白天,其余夜间。

### 改的时候要知道

- 想改时间段 → 改 `6` 和 `18` 这两个数字
- 想更精确(按真实日出日落)→ 需要接日出日落 API 或用 `astral` 之类的库算,复杂度上升。**先从简单的开始,需要再升级**——这是好的工程习惯。
- 注意:依赖系统时间正确。如果树莓派没联网同步时间,判断会错。你的设备联网,所以没问题。

---

## 八、两套主题字典:为什么这样组织颜色

### 设计

```python
DAY   = dict(bg=..., track=..., accent=..., white=..., grey=..., mem=...)
NIGHT = dict(bg=..., track=..., accent=..., white=..., grey=..., mem=...)
```

把所有颜色按"用途"命名(背景、进度条槽、强调色、主文字、次文字、内存色),日夜各一套。
用的时候 `theme(mix, "accent")` 自动返回日夜之间插值后的颜色。

### 为什么这样好

- **加颜色只改字典**:想多一个颜色,在两个字典里各加一行,画图时用 `theme(mix,"新名字")`。
- **改配色不碰画图逻辑**:觉得夜间太暗,只改 `NIGHT` 字典的值,画图代码一行不动。
- **日夜自动一致**:因为画图只认 `theme()`,改字典就全局生效。

### 改的时候要知道

- 调整某个颜色 → 改 `DAY`/`NIGHT` 字典对应的值,不用动别处
- 加新的彩色元素 → 字典加项 + 画图时 `theme(mix, "项名")`

---

## 九、温度变色:为什么单独处理

温度色不在主题字典里,因为它**不只随昼夜变,还随温度值变**(绿→黄→红)。
```python
def temp_color(t, mix): ...   # 先按温度算颜色,再按 mix 夜间压暗
```
逻辑:先根据温度(35→绿,55→黄,75→红)算出颜色,再用 `mix` 让夜间整体压暗一点。

### 改的时候要知道

- 改温度阈值 → 改 `temp_color` 里的 `35/55/75`
- 改颜色档位 → 改里面的 RGB 值

---

## 十、运行架构:为什么做成 systemd 服务

### 设计

panel.py 放在 `/usr/local/bin/`,用 systemd 服务 `panel.service` 管理。

### 为什么用服务而不是开机跑脚本

- `Restart=always`:程序崩了自动重启,面板常驻不怕挂
- `enable`:开机自启
- 能用 `systemctl start/stop/status` 统一管理
- 和 `display-mode` 脚本配合:`panel` 模式 = 终端模式 + enable 这个服务

### 三模式怎么共存(关键)

三个模式都要独占 fb0,不能同时跑:
- `desktop` = 图形目标,disable 面板服务
- `console` = 终端目标,disable 面板服务
- `panel` = 终端目标 + enable 面板服务(面板全屏盖住终端)

### 改的时候要知道

- 改了 panel.py 后,要 `sudo cp ~/panel.py /usr/local/bin/panel.py` 再 `sudo systemctl restart panel.service` 才生效
- 调试时先 `systemctl stop panel.service` 停服务,再手动 `sudo python3 ~/panel.py` 看效果,避免两个一起抢屏

---

## 十一、踩过的相关坑

| 坑 | 现象 | 原因 | 解决 |
|---|---|---|---|
| framebuffer 字节序 | 颜色红蓝互换 | RGBX vs BGRX | 调字节对应 |
| tty 光标透出 | 面板上有光标闪 | 底层 tty 光标画在 fb0 上 | cmdline 加 `vt.global_cursor_default=0` |
| 服务抢屏 | 手动跑面板花屏 | 服务和手动同时写 fb0 | 调试前先 stop 服务 |
| 改了不生效 | 重启服务还是旧的 | 没 cp 到 /usr/local/bin | 改完要 cp + restart |

---

## 十二、以后可以加的功能(及大概怎么加)

| 功能 | 怎么加 |
|---|---|
| 网络速率 | 读 `/sys/class/net/wlan0/statistics/rx_bytes`,两次采样差值 ÷ 时间 |
| CPU 各核 | 读 `/proc/stat` 的 `cpu0/cpu1/...` 行,分别算 |
| 磁盘占用 | `shutil.disk_usage("/")`,套个进度条 |
| 精确日出日落 | 用 `astral` 库按经纬度算,替换第七节的小时判断 |
| 多页轮播 | 加一个页码变量,定时切换画不同内容 |

**加任何新数据项的套路**:写个 `get_xxx()` 采集函数 → 在数据字典加一项 → 画图区加对应显示(文字或进度条)→ 想平滑就接 `disp` 插值。

---

## 核心思想回顾(最该记住的)

1. **屏幕是内存,画图就是写字节**——framebuffer 的本质
2. **采集慢、渲染快**——动画顺滑的结构
3. **lerp 插值**——一切平滑变化(进度条、主题)的通用工具
4. **时间 + sin/cos**——一切动效的通用公式
5. **同位置画两套 + 强度此消彼长**——状态形变(太阳↔月亮)
6. **颜色用字典按用途组织**——改配色不碰逻辑
7. **服务化 + 模式互斥**——三模式共存一块屏

这七点是真正可迁移的。具体代码会忘,这些思想换个项目还能用。

---

*基于一次真实的面板开发整理。代码会改,设计思想长存。*